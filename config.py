"""
config.py — Общие константы приложения.

Этот файл хранит значения, которые используются в нескольких модулях.
Вынесены сюда, чтобы не дублировать одно и то же число в разных местах (DRY).
Если нужно изменить размер страницы — правим только здесь,
и изменение автоматически применится во всех модулях.
"""

# Количество фильмов на одной странице результатов поиска.
# Используется в main.py (пагинация) и mysql_connector.py (LIMIT в SQL-запросе).
PAGE_SIZE: int = 10

# Ширина всех таблиц в символах.
# Используется в formatter.py для рамок и вычисления ширины колонок.
TABLE_W: int = 70
